# Assets

Place images, sounds, fonts, level files, and other local game assets here.

Reference them with relative paths from your p5.js code, for example:

```javascript
playerImage = loadImage("assets/player.png");
collectSound = loadSound("assets/collect.wav");
```
